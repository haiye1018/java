package com.kinginsai.CRM.intercepertor;import com.kinginsai.CRM.login.LoginController;import org.springframework.stereotype.Component;import org.springframework.web.servlet.HandlerInterceptor;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import java.text.DateFormat;import java.text.SimpleDateFormat;import java.util.Date;@Componentpublic class LoginInterceptor implements HandlerInterceptor {    private final Logger log = LoggerFactory.getLogger(this.getClass());    @Override    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {        String requestURI = request.getRequestURI();        //log.info("拦截的请求路径：{}",requestURI);        //登录检查逻辑        HttpSession session = request.getSession();        Object sessionId = session.getAttribute("sessionId");        if(sessionId != null && LoginController.sessionMap.containsKey((String)sessionId)){            Session sess = LoginController.sessionMap.get(sessionId);            DateFormat df = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");            Date d1 = sess.getCreateTime();            Date d2 = new Date();            long diff = d2.getTime() - d1.getTime();            if(diff/1000 > sess.getMaxInactiveInterval()){                LoginController.sessionMap.remove(sessionId);            } else {                sess.setCreateTime(d2);                return true;            }        }        request.setAttribute("msg","请先登录");        request.getRequestDispatcher("/login").forward(request, response);        return false;    }}